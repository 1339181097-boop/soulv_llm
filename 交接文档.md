# soulv_llm 项目交接文档

更新日期：2026-06-05  
交接目标：让下一位同事按“数据 pipeline -> Stage1 -> Stage2 -> 部署/评测/云端”顺序，能直接理解现状并继续推进。

## 0. 一句话结论

这个仓库是 TripAI 旅行助手模型训练工程。当前主线已经从“纯自然语言 SFT”推进到“高德 AMap 工具调用 / function calling 对齐”。

最重要的判断：

- `8B` 是低成本全链路 MVP 验证模型，不是最终生产主力。
- `32B` 是正式产品能力主线。
- `Stage1` 负责六类旅游自然语言能力。
- `Stage2` 在 Stage1 merged base 上继续做高德工具调用能力。
- `src/eval/` 负责 Stage1 自然语言回归。
- `src/tool_eval/` 负责 Stage2 工具调用专项评测。

当前最该接着做的事情：

1. 不要盲目重跑旧入口脚本，先看本文的“已知坑”。
2. 确认云端 32B 权重、数据、dataset registration、训练产物是否真实存在。
3. 若继续 32B 正式链路，按本文 Stage1 32B -> merge -> Stage2 32B -> merge -> vLLM/gateway -> eval 的顺序推进。
4. 所有 key 只放环境变量，不写进文档、脚本、Git 或 shell history。

## 1. 仓库结构

核心目录：

| 路径 | 用途 |
|---|---|
| `configs/` | LLaMA-Factory 训练、merge、dataset registration 配置 |
| `scripts/` | 环境、训练、merge、部署入口脚本 |
| `src/data_pipeline/` | Stage1/Stage2 数据清洗、混合、导出、badcase 修复 |
| `src/data_pipeline/handlers/` | Stage1 六类自然语言 handler |
| `src/tool_use/` | 高德工具协议、真实 AMap client、工具编排器 |
| `src/tool_eval/` | Stage2 工具调用专项评测集、runner、打分脚本 |
| `src/eval/` | Stage1 冻结自然语言评测集、规则打分、LLM Judge |
| `src/deploy/` | 前端网关和网页 |
| `data/` | 本地 raw/processed/final/handoff/badcase 数据产物 |
| `docs/` | 方案、数据需求、部署、训练指南 |
| `tests/` | 数据 pipeline、tool-use、部署网关等单测 |

顺序入口脚本：

| 脚本 | 当前作用 |
|---|---|
| `scripts/00_setup_lf_env.sh` | 创建/安装训练环境 `lf` |
| `scripts/00_setup_qwen_env.sh` | 创建/安装 vLLM 推理环境 `qwen` |
| `scripts/00_prepare_aliyun_layout.sh` | 创建云端目录和可选软链接 |
| `scripts/00_doctor_envs.sh` | 检查 GPU、torch、LLaMA-Factory、vLLM |
| `scripts/01_run_pipeline.sh` | 早期 Stage1 pipeline 入口，路径口径已旧，见“已知坑” |
| `scripts/02_run_sft.sh` | SFT 训练统一入口 |
| `scripts/03_run_vllm_api.sh` | vLLM OpenAI-compatible 服务入口 |
| `scripts/04_merge_stage1_for_stage2.sh` | Stage1 LoRA merge 成 Stage2 起始模型 |
| `scripts/05_build_stage2_amap_data.sh` | 早期 Stage2 builder 入口，当前 v2 主线不只靠它 |
| `scripts/06_merge_stage2_for_deploy.sh` | Stage2 LoRA merge 成可部署模型 |
| `scripts/07_run_frontend_gateway.sh` | 前端网页 + 同源网关入口 |

## 2. 服务器和资源约定

仓库没有记录具体云服务器 IP、登录账号、密码或云控制台信息。需要从公司云账号/运维/上一任交接中确认。仓库里能确认的是路径和环境约定。

云端默认路径：

| 路径 | 用途 |
|---|---|
| `/root/soulv_llm` | 代码仓库，只放代码、轻量配置、文档 |
| `/root/soulv_assets` | 大资源总目录，不进 Git |
| `/root/soulv_assets/models/modelscope` | ModelScope 模型缓存 |
| `/root/soulv_assets/models/huggingface` | Hugging Face 模型缓存 |
| `/root/soulv_assets/runs/checkpoints` | LoRA checkpoint |
| `/root/soulv_assets/runs/merged` | merge 后模型 |
| `/root/llama-factory` | LLaMA-Factory 官方仓库 |
| `/root/llama-factory/data` | LLaMA-Factory 训练数据和 dataset registration |

建议写入云端 `~/.bashrc` 的环境变量：

```bash
export MODELSCOPE_CACHE=/root/soulv_assets/models/modelscope
export HF_HOME=/root/soulv_assets/models/huggingface
export SOULV_DATASET_DIR=/root/soulv_assets/dataset
export SOULV_RUNS_DIR=/root/soulv_assets/runs
```

运行时 key：

| 环境变量 | 用途 |
|---|---|
| `AMAP_API_KEY` | 真实高德工具执行 |
| `DASHSCOPE_API_KEY` | qwen-plus Judge / DeepSeek badcase scoring |
| `VLLM_API_KEY` | vLLM 服务可选鉴权 |
| `UPSTREAM_VLLM_API_KEY` | 网关访问 vLLM 时可选鉴权 |

不要把任何 key 写进 Git、文档、命令历史或前端源码。

## 3. 本地与云端环境

本地开发环境主要用于数据清洗、单测、文档和轻量脚本：

```bash
pip install -r requirements.txt
pytest -q
```

云端训练环境 `lf`：

```bash
bash /root/soulv_llm/scripts/00_setup_lf_env.sh
```

关键版本固定在 `requirements-lf.txt`：

- Python 3.11
- LLaMA-Factory `v0.9.4`
- torch `2.6.0 + cu124`
- transformers `4.51.3`
- deepspeed `0.16.4`
- bitsandbytes `0.48.2`

云端推理环境 `qwen`：

```bash
bash /root/soulv_llm/scripts/00_setup_qwen_env.sh
```

关键版本固定在 `requirements-qwen.txt`：

- vLLM `0.10.2`
- transformers `4.56.2`
- tokenizers `>=0.22,<0.24`
- modelscope `>=1.34,<2`
- qwen-agent `>=0.0.29`

新服务器初始化推荐顺序：

```bash
bash /root/soulv_llm/scripts/00_setup_lf_env.sh
bash /root/soulv_llm/scripts/00_setup_qwen_env.sh
bash /root/soulv_llm/scripts/00_prepare_aliyun_layout.sh
bash /root/soulv_llm/scripts/00_doctor_envs.sh
```

目标机器口径：

- 32B 主线默认按 `2 x NVIDIA L20 48G`
- 32B 训练默认 `4bit QLoRA + 双卡分布式 + deepspeed zero2`
- 8B 历史链路可单卡/更低成本验证

## 4. 数据总览

当前本地 `data/` 目录已有这些主要子目录：

| 目录 | 用途 |
|---|---|
| `data/raw_stage1/` | Stage1 六类原始候选 |
| `data/processed_stage1/` | Stage1 严格清洗后候选 |
| `data/raw_stage2/` | Stage2 工具调用原始候选 |
| `data/processed_stage2/` | Stage2 分桶严格清洗后候选 |
| `data/final/` | 当前最终训练数据和报告 |
| `data/tool_use/` | Stage2 source dataset |
| `data/badcase_fix/` | badcase 修复扩展、去重、评分产物 |
| `data/handoff/` | 给数据部门标注/扩展的交接文件 |
| `data/reports/` | 数据清洗报告 |
| `data/eval/` | 评测 badcase 等数据 |

当前最重要的 final 产物：

| 文件 | 当前状态 |
|---|---|
| `data/final/stage1_general_sft.json` | Stage1 六类自然语言正式集，9919 条，校验通过 |
| `data/final/stage1_general_sft_with_badcase_repair_15pct.json` | Stage1 + badcase repair 混合版，11669 条，repair 占比约 15% |
| `data/final/stage1_general_sft_with_badcase_repair_15pct_lf.json` | LLaMA-Factory 训练用 repair 混合版 |
| `data/final/stage1_badcase_repair_sft.json` | badcase repair 单独训练集 |
| `data/final/stage2_amap_tool_use_sft.json` | Stage2 AMap tool-use 训练集，目标 3200，实际 3092，校验通过 |
| `data/final/stage2_amap_tool_use_report.json` | Stage2 final mix 报告 |

当前 Stage1 final 统计：

| task_type | 条数 |
|---|---:|
| `guide_generation` | 650 |
| `travel_qa` | 3250 |
| `hotel_recommendation` | 1900 |
| `traffic_planning` | 1919 |
| `persona_understanding` | 1300 |
| `multi_turn_dialogue` | 900 |
| 合计 | 9919 |

当前 Stage2 final 统计：

| task_type | 目标 | 实际 |
|---|---:|---:|
| `clarify_then_call` | 1200 | 1170 |
| `two_step_chain` | 800 | 722 |
| `tool_result_grounded_answer` | 400 | 400 |
| `single_tool_call` | 240 | 240 |
| `slot_filling_tool_call` | 240 | 240 |
| `tool_failure_fallback` | 160 | 160 |
| `no_tool_needed` | 160 | 160 |
| 合计 | 3200 | 3092 |

Stage2 短缺 108 条，主要来自：

- `clarify_then_call` 短 30
- `two_step_chain` 短 78

## 5. 已知坑：不要盲跑旧入口

这是交接里最重要的工程风险。

### 5.1 Stage1 旧入口路径不等于当前数据目录

`scripts/01_run_pipeline.sh` 仍然引用早期路径，例如：

- `data/raw/...`
- `data/processed/...`

但当前实际数据目录是：

- `data/raw_stage1/...`
- `data/processed_stage1/...`

另外，`src/data_pipeline/data_mixer.py` 当前源码默认读取 `data/processed/<filename>`，但本地现有正式输入文件在 `data/processed_stage1/`。因此如果下一位要完全重跑 Stage1 final，需要先统一路径口径，不要直接在生产数据目录上盲跑。

最稳策略：

1. 沿用当前 `data/final/stage1_general_sft.json` 作为 Stage1 正式集。
2. 如需重建，先明确是改脚本默认目录，还是临时准备 `data/processed/` 镜像。
3. 重建后必须重新检查 `data/final/stage_mix_report.json`。

### 5.2 Stage2 当前主线不是只跑 `05_build_stage2_amap_data.sh`

`scripts/05_build_stage2_amap_data.sh` 调的是 `src/data_pipeline/build_stage2_amap_tool_use.py`。这是早期 builder，默认从 Stage1 processed 数据合成工具调用样本。

当前本地 final 数据实际来自 v2 质量优先路径：

```text
data/raw_stage2/*
-> clean_stage2_*.py
-> data/processed_stage2/*
-> src/data_pipeline/finalize_stage2_processed.py
-> data/final/stage2_amap_tool_use_sft.json
```

所以当前如果继续补 Stage2 v2，不要只跑 `scripts/05_build_stage2_amap_data.sh`。应按本文 Stage2 章节跑分桶清洗和 finalization。

### 5.3 two-step 清洗器默认输出名有历史遗留

`clean_stage2_two_step_chain.py` 当前默认输出名包含 `two_step_chain_v2_repaired...`，但最终合并报告里实际使用的是：

```text
data/processed_stage2/two_step_chain.jsonl
```

下一位如果要补 two-step 数据，务必以 `data/final/stage2_amap_tool_use_report.json` 的 `input_files.two_step_chain.file` 为最终口径。

## 6. 数据 Pipeline：Stage1

Stage1 目标是六类自然语言 SFT，不包含工具调用轨迹。

六类任务：

1. `guide_generation`
2. `travel_qa`
3. `hotel_recommendation`
4. `traffic_planning`
5. `persona_understanding`
6. `multi_turn_dialogue`

Stage1 训练格式：

```json
{
  "messages": [
    {"role": "system", "content": "..."},
    {"role": "user", "content": "..."},
    {"role": "assistant", "content": "..."}
  ]
}
```

Stage1 数据中不应该出现：

- `tools`
- `tool_calls`
- `function_call`
- `observation`
- `expected_behavior`
- `messages_with_answer`

当前正式输入池：

| 文件 | task_type | 目标 |
|---|---|---:|
| `data/processed_stage1/sft_guide_generation_2026_04_24_strict.jsonl` | `guide_generation` | 650 |
| `data/processed_stage1/sft_travel_qa_2026_04_22_strict.jsonl` | `travel_qa` | 3250 |
| `data/processed_stage1/sft_hotel_recommendation_0423_strict.jsonl` | `hotel_recommendation` | 1900 |
| `data/processed_stage1/sft_traffic_planning_strict_round2_final.jsonl` | `traffic_planning` | 1919 |
| `data/processed_stage1/sft_persona_understanding_strict_round2.jsonl` | `persona_understanding` | 1300 |
| `data/processed_stage1/sft_multi_turn_dialogue_2026_4_22_strict_round2.jsonl` | `multi_turn_dialogue` | 900 |

核心代码：

| 文件 | 用途 |
|---|---|
| `src/data_pipeline/handlers/handler_*.py` | 六类原始数据转 ChatML |
| `src/data_pipeline/guide_generation_strict_cleaner.py` | guide_generation 严格清洗 |
| `src/data_pipeline/travel_qa_round2_cleaner.py` | travel_qa 二轮严格清洗 |
| `src/data_pipeline/traffic_planning_round2_cleaner.py` | traffic_planning 二轮严格清洗 |
| `src/data_pipeline/multi_turn_dialogue_round2_cleaner.py` | multi_turn_dialogue 二轮严格清洗 |
| `src/data_pipeline/data_mixer.py` | 六类混合、token gate、输出 Stage1 final |

Stage1 mix 质量门：

- 全局 shuffle，最多连续同类 8 条。
- `guide_generation`、`multi_turn_dialogue` 做 token share 控制，防止长文压制其他任务。
- `chatml_error_count = 0`
- `strict_error_count = 0`
- `duplicate_primary_id_count = 0`

当前 `stage_mix_report.json` 已通过。

## 7. 数据 Pipeline：Badcase Repair

Badcase repair 是为修复 Stage2 之后的自然语言回归问题准备的，不是替代 Stage1 主集。

背景：

- 评测对象：`qwen3_32b_stage2_amap_tool_use_merged_v2`
- Stage1 冻结评测集：6 类共 360 条。
- 自动评测后抽取 90 条文本能力 badcase。
- badcase 主要集中在 `traffic_planning`、`travel_qa`、`persona_understanding`、`multi_turn_dialogue`。

重要原则：

- 原始 badcase 留作回归测试，不建议直接进训练集。
- 训练修复优先使用同类变体/扩展样本，避免污染评测集。

关键文件：

| 文件 | 用途 |
|---|---|
| `data/handoff/qwen3_32b_stage1_text_full_badcases_for_labeling.jsonl` | 给数据部门标注的 Stage1 badcase |
| `data/handoff/qwen3_32b_stage2_text_full_badcases_for_labeling.jsonl` | 给数据部门标注的 Stage2 text badcase |
| `docs/badcase_labeling_and_expansion_guide.md` | 标注与扩展要求 |
| `data/badcase_fix/qwen3_32b_stage1_text_badcase_repair_expanded_v2.jsonl` | 扩展样本 |
| `data/badcase_fix/qwen3_32b_stage1_text_badcase_repair_expanded_v2.local_clean_final.jsonl` | 本地严格清洗后样本 |
| `data/badcase_fix/qwen3_32b_stage1_text_badcase_repair_expanded_v2.final_kept.jsonl` | DashScope/DeepSeek 评分后保留样本 |
| `data/final/stage1_badcase_repair_sft.json` | 转成 Stage1 ChatML 的 repair 集 |
| `data/final/stage1_general_sft_with_badcase_repair_15pct_lf.json` | LLaMA-Factory 训练用混合版 |

关键脚本：

```bash
python src/data_pipeline/dedup_badcase_repairs.py
python src/data_pipeline/clean_badcase_repairs.py
python src/data_pipeline/score_badcase_repairs_with_dashscope.py
python src/data_pipeline/prepare_badcase_repair_sft.py
python src/data_pipeline/mix_stage1_badcase_repair.py
```

当前评分结果：

- input: 727
- keep: 563
- review: 164
- 平均 overall score: 4.381
- 平均 confidence: 0.901
- 自动 keep 条件：`min_score >= 4` 且 `min_confidence >= 0.7`

评分脚本使用：

```bash
export DASHSCOPE_API_KEY=<your-key>

python src/data_pipeline/score_badcase_repairs_with_dashscope.py \
  --input data/badcase_fix/qwen3_32b_stage1_text_badcase_repair_expanded_v2.local_clean_final.jsonl \
  --scored data/badcase_fix/qwen3_32b_stage1_text_badcase_repair_expanded_v2.final_scored.jsonl \
  --kept data/badcase_fix/qwen3_32b_stage1_text_badcase_repair_expanded_v2.final_kept.jsonl \
  --review data/badcase_fix/qwen3_32b_stage1_text_badcase_repair_expanded_v2.final_review.jsonl \
  --summary data/badcase_fix/qwen3_32b_stage1_text_badcase_repair_expanded_v2.final_score_summary.json \
  --resume
```

不要用 `--api-key <key>`，优先用 `DASHSCOPE_API_KEY`，避免 key 进 shell history。

## 8. 数据 Pipeline：Stage2 AMap Tool-Use

Stage2 目标不是从零教模型工具调用，而是把 Qwen3 已有 OpenAI-compatible tool calling 能力对齐到 TripAI 高德工具协议。

当前冻结工具：

1. `amap_geocode`
2. `amap_search_poi`
3. `amap_plan_route`

协议定义：

```text
src/tool_use/protocol.py
```

固定边界：

- 最多两步工具链。
- 允许 `amap_geocode -> amap_plan_route`。
- 允许 `amap_geocode -> amap_search_poi`。
- 禁止任意三步或更多工具链。
- 缺参数先澄清。
- 无需工具时直接自然回答。
- 工具失败/空结果时明确 fallback，不编造。

Stage2 训练格式是 LLaMA-Factory `sharegpt + tools`：

```json
{
  "id": "...",
  "task_type": "clarify_then_call",
  "scene": "amap_plan_route",
  "expected_behavior": "should_clarify",
  "tools": "[...]",
  "conversations": [
    {"from": "system", "value": "..."},
    {"from": "human", "value": "..."},
    {"from": "gpt", "value": "..."},
    {"from": "function_call", "value": "{\"name\":\"amap_plan_route\",\"arguments\":{...}}"},
    {"from": "observation", "value": "{\"status\":\"success\",\"data\":{...}}"},
    {"from": "gpt", "value": "..."}
  ]
}
```

Stage2 v2 目标配比：

| 桶 | 目标 | 目的 |
|---|---:|---|
| `clarify_then_call` | 1200 | 修复缺参过调用 |
| `two_step_chain` | 800 | 显式训练 geocode 后接 route/search |
| `tool_result_grounded_answer` | 400 | 修复最终回答风格和单位转换 |
| `single_tool_call` | 240 | 保留直接调用能力 |
| `slot_filling_tool_call` | 240 | 保留参数抽取能力 |
| `tool_failure_fallback` | 160 | 保留失败回退能力 |
| `no_tool_needed` | 160 | 防止凡事调工具 |

Stage2 数据需求和生成 prompt：

- `docs/stage2_amap_tool_use_v2_data_requirements.md`
- `docs/stage2_amap_qwen_plus_data_generation_system_prompt.md`

当前 v2 数据生成方式：

1. 数据部门或脚本按 prompt 生成 `data/raw_stage2/*.jsonl`。
2. 每个桶走对应 cleaner。
3. `clarify_then_call` 可通过 merge 脚本合并多轮 raw 和 seed。
4. `finalize_stage2_processed.py` 做最终质量优先合并。
5. 最终输出 `data/final/stage2_amap_tool_use_sft.json`。

主要清洗脚本：

```bash
python src/data_pipeline/clean_stage2_single_tool_call.py
python src/data_pipeline/clean_stage2_slot_filling_tool_call.py
python src/data_pipeline/clean_stage2_clarify_then_call.py --allow-partial
python src/data_pipeline/merge_stage2_clarify_then_call_strict.py
python src/data_pipeline/clean_stage2_tool_result_grounded_answer.py
python src/data_pipeline/clean_stage2_tool_failure_fallback.py
python src/data_pipeline/clean_stage2_no_tool_needed.py
python src/data_pipeline/clean_stage2_two_step_chain.py
```

最终合并：

```bash
python src/data_pipeline/finalize_stage2_processed.py
```

最终合并实际读取的文件以报告为准：

| task_type | finalization 输入 |
|---|---|
| `clarify_then_call` | `data/processed_stage2/clarify_then_call_strict_merged.jsonl` |
| `two_step_chain` | `data/processed_stage2/two_step_chain.jsonl` |
| `tool_result_grounded_answer` | `data/processed_stage2/tool_result_grounded_answer.jsonl` |
| `single_tool_call` | `data/processed_stage2/single_tool_call.jsonl` |
| `slot_filling_tool_call` | `data/processed_stage2/slot_filling_tool_call.jsonl` |
| `tool_failure_fallback` | `data/processed_stage2/tool_failure_fallback.jsonl` |
| `no_tool_needed` | `data/processed_stage2/no_tool_needed.jsonl` |

校验：

```bash
python src/data_pipeline/validate_tool_use_dataset.py \
  --file data/final/stage2_amap_tool_use_sft.json \
  --format sharegpt \
  --preview
```

如果只是早期 source dataset 重新导出：

```bash
python src/data_pipeline/export_stage2_amap_tool_use.py
```

注意：`export_stage2_amap_tool_use.py` 默认读 `data/tool_use/stage2_amap_tool_use_source.json`，这是早期 source/export 路径，不等同于当前 v2 processed final 主线。

## 9. Stage1 训练

训练入口：

```bash
bash scripts/02_run_sft.sh <stage> <profile>
```

### 9.1 8B 历史 MVP 链路

Stage1 8B：

```bash
bash scripts/02_run_sft.sh stage1
```

配置：

```text
configs/llamafactory_stage1_sft.yaml
```

默认：

- base: `/root/soulv_llm/models/base_models/Qwen3-8B-Instruct`
- dataset: `soulv_stage1_general_sft`
- output: `/root/soulv_assets/runs/checkpoints/qwen3_8b_stage1_general_sft`
- `cutoff_len=2048`
- `4bit LoRA`

Stage1 8B merge：

```bash
bash scripts/04_merge_stage1_for_stage2.sh stage1
```

输出：

```text
/root/soulv_assets/runs/merged/stage1_merged_base
```

### 9.2 32B 正式主线

Stage1 32B smoke：

```bash
bash scripts/02_run_sft.sh stage1_32b smoke
```

配置：

```text
configs/llamafactory_stage1_32b_smoke_sft.yaml
```

默认：

- base: `/root/soulv_assets/models/modelscope/models/Qwen/Qwen3-32B`
- dataset: `soulv_stage1_general_sft`
- `max_samples=800`
- `max_steps=150`
- output: `/root/soulv_assets/runs/checkpoints/qwen3_32b_stage1_general_sft_smoke`

Stage1 32B formal：

```bash
bash scripts/02_run_sft.sh stage1_32b formal
```

配置：

```text
configs/llamafactory_stage1_32b_formal_sft.yaml
```

默认：

- base: `/root/soulv_assets/models/modelscope/models/Qwen/Qwen3-32B`
- dataset: `soulv_stage1_general_sft`
- output: `/root/soulv_assets/runs/checkpoints/qwen3_32b_stage1_general_sft`
- `cutoff_len=2048`
- `lora_rank=64`
- `gradient_accumulation_steps=8`
- `num_train_epochs=3`
- `learning_rate=5e-5`
- `deepspeed=/root/soulv_llm/configs/deepspeed_zero2.json`

Stage1 32B merge：

```bash
bash scripts/04_merge_stage1_for_stage2.sh stage1_32b
```

配置：

```text
configs/llamafactory_stage1_32b_merge_for_stage2.yaml
```

输出：

```text
/root/soulv_assets/runs/merged/qwen3_32b_stage1_merged_base
```

### 9.3 Stage1 badcase repair 训练

如果要做文本能力修复：

```bash
bash scripts/02_run_sft.sh stage1_32b repair
```

配置：

```text
configs/llamafactory_stage1_32b_badcase_repair_sft.yaml
```

默认：

- 起点：`/root/soulv_assets/runs/merged/qwen3_32b_stage1_merged_base`
- dataset: `soulv_stage1_general_sft_badcase_repair_15pct_lf`
- output: `/root/soulv_assets/runs/checkpoints/qwen3_32b_stage1_general_sft_badcase_repair_15pct`
- learning rate: `2e-5`

Repair merge：

```bash
bash scripts/04_merge_stage1_for_stage2.sh stage1_32b_repair
```

配置：

```text
configs/llamafactory_stage1_32b_badcase_repair_merge_for_stage2.yaml
```

## 10. Stage2 训练

Stage2 起点是 Stage1 merged base，不建议直接拿原始 base 开训。

### 10.1 8B 历史 MVP 链路

训练：

```bash
bash scripts/02_run_sft.sh stage2_amap
```

配置：

```text
configs/llamafactory_stage2_amap_tool_use_sft.yaml
```

默认：

- base: `/root/soulv_assets/runs/merged/stage1_merged_base`
- dataset: `soulv_stage2_amap_tool_use_sft`
- output: `/root/soulv_assets/runs/checkpoints/qwen3_8b_stage2_amap_tool_use`
- `cutoff_len=4096`

Merge：

```bash
bash scripts/06_merge_stage2_for_deploy.sh stage2_amap
```

输出：

```text
/root/soulv_assets/runs/merged/qwen3_8b_stage2_amap_tool_use_merged
```

### 10.2 32B 正式主线

Stage2 32B smoke：

```bash
bash scripts/02_run_sft.sh stage2_amap_32b smoke
```

配置：

```text
configs/llamafactory_stage2_32b_amap_tool_use_smoke_sft.yaml
```

默认：

- base: `/root/soulv_assets/runs/merged/qwen3_32b_stage1_merged_base`
- dataset: `soulv_stage2_amap_tool_use_sft`
- `max_samples=320`
- `max_steps=120`
- output: `/root/soulv_assets/runs/checkpoints/qwen3_32b_stage2_amap_tool_use_smoke`

Stage2 32B formal：

```bash
bash scripts/02_run_sft.sh stage2_amap_32b formal
```

配置：

```text
configs/llamafactory_stage2_32b_amap_tool_use_formal_sft.yaml
```

默认：

- base: `/root/soulv_assets/runs/merged/qwen3_32b_stage1_merged_base`
- dataset: `soulv_stage2_amap_tool_use_sft`
- output: `/root/soulv_assets/runs/checkpoints/qwen3_32b_stage2_amap_tool_use`
- `cutoff_len=4096`
- `learning_rate=3e-5`
- `num_train_epochs=2`
- `deepspeed=/root/soulv_llm/configs/deepspeed_zero2.json`

Stage2 32B merge：

```bash
bash scripts/06_merge_stage2_for_deploy.sh stage2_amap_32b
```

配置：

```text
configs/llamafactory_stage2_32b_merge_for_deploy.yaml
```

输出：

```text
/root/soulv_assets/runs/merged/qwen3_32b_stage2_amap_tool_use_merged
```

## 11. 远端 dataset registration

LLaMA-Factory 训练配置里写的是 dataset 名，不是直接文件路径。因此云端必须在 `/root/llama-factory/data/dataset_info.json` 里注册。

Stage1 registration 来源：

```text
configs/llamafactory_dataset_info_stage1_general_sft.json
```

关键数据集名：

- `soulv_stage1_general_sft`
- `soulv_stage1_general_sft_badcase_repair_15pct`
- `soulv_stage1_general_sft_badcase_repair_15pct_lf`

Stage2 registration 来源：

```text
configs/llamafactory_dataset_info_stage2_amap_tool.json
```

关键数据集名：

- `soulv_stage2_amap_tool_use_sft`

云端数据文件建议放到：

```text
/root/llama-factory/data/stage1_general_sft.json
/root/llama-factory/data/stage1_general_sft_with_badcase_repair_15pct_lf.json
/root/llama-factory/data/stage2_amap_tool_use_sft.json
```

注意：

- Stage1 的 columns 是 `messages: messages`。
- Stage2 的 columns 是 `messages: conversations`，`tools: tools`。
- Stage2 tags 使用 `from/value/human/gpt/system/function_call/observation`。
- 每次换数据文件名或数据集名，都要同步 dataset registration 和 YAML。

## 12. 部署：vLLM

vLLM 启动脚本：

```text
scripts/03_run_vllm_api.sh
```

脚本默认能力：

- 显式指定 tokenizer。
- 显式指定 served model name。
- 默认 `--generation-config vllm`。
- 默认开启 Qwen3 reasoning parser：`--reasoning-parser qwen3`。
- 默认开启 auto tool choice：`--enable-auto-tool-choice --tool-call-parser hermes`。
- 支持 `VLLM_API_KEY`。

32B base 启动示例：

```bash
MODEL_VARIANT=32b \
MODEL_PATH=/root/soulv_assets/models/modelscope/models/Qwen/Qwen3-32B \
TOKENIZER_PATH=/root/soulv_assets/models/modelscope/models/Qwen/Qwen3-32B \
SERVED_MODEL_NAME=qwen3_32b_base \
HOST=127.0.0.1 \
PORT=8000 \
bash scripts/03_run_vllm_api.sh
```

32B Stage2 merged 启动示例：

```bash
MODEL_VARIANT=32b \
MODEL_PATH=/root/soulv_assets/runs/merged/qwen3_32b_stage2_amap_tool_use_merged \
TOKENIZER_PATH=/root/soulv_assets/models/modelscope/models/Qwen/Qwen3-32B \
SERVED_MODEL_NAME=qwen3_32b_stage2_amap_tool_use \
HOST=127.0.0.1 \
PORT=8000 \
bash scripts/03_run_vllm_api.sh
```

8B 历史 merged 启动示例：

```bash
MODEL_VARIANT=8b \
MODEL_PATH=/root/soulv_assets/runs/merged/qwen3_8b_stage2_amap_tool_use_merged \
TOKENIZER_PATH=/root/soulv_assets/models/modelscope/models/Qwen/Qwen3-8B \
SERVED_MODEL_NAME=qwen3_8b_stage2_amap_tool_use \
HOST=127.0.0.1 \
PORT=8000 \
bash scripts/03_run_vllm_api.sh
```

如果要直接让外部访问 vLLM，可以 `HOST=0.0.0.0`，但推荐不要直接暴露 8000，而是走前端网关。

## 13. 部署：前端网关

前端网关入口：

```text
scripts/07_run_frontend_gateway.sh
```

网关能力：

- 托管 `src/deploy/web/index.html`。
- 代理 `/v1/*` 到上游 vLLM。
- 提供 `/api/tool-orchestrate` 做端到端工具执行。
- 浏览器不需要知道 vLLM API key。
- 其他人只访问 `http://<server-ip>:7860`。

启动：

```bash
export AMAP_API_KEY=<your-amap-key>

HOST=0.0.0.0 \
PORT=7860 \
UPSTREAM_VLLM_BASE_URL=http://127.0.0.1:8000 \
DEFAULT_MODEL_NAME=qwen3_32b_stage2_amap_tool_use \
bash scripts/07_run_frontend_gateway.sh
```

如果 vLLM 设置了 API key：

```bash
export UPSTREAM_VLLM_API_KEY=<your-vllm-key>
```

推荐公网拓扑：

1. vLLM 监听 `127.0.0.1:8000`。
2. 网关监听 `0.0.0.0:7860`。
3. 安全组/防火墙只放行 `7860`。
4. 外部访问 `http://<server-ip>:7860`。
5. 外部 OpenAI-compatible API 使用 `http://<server-ip>:7860/v1`。

## 14. 真实高德工具执行

工具执行相关代码：

| 文件 | 用途 |
|---|---|
| `src/tool_use/protocol.py` | 工具 schema、系统提示、允许工具链 |
| `src/tool_use/amap_client.py` | 真实高德 Web API client |
| `src/tool_use/orchestrator.py` | OpenAI-compatible tool orchestration loop |
| `src/deploy/frontend_server.py` | 网关里接 `/api/tool-orchestrate` |

`AmapClient` 会访问：

```text
https://restapi.amap.com
```

没有 `AMAP_API_KEY` 时不会编造，会返回：

```json
{"status": "error", "reason": "missing_amap_api_key"}
```

`ToolCallingOrchestrator` 行为：

1. 第一次请求模型时传入 tools。
2. 解析模型返回的 `tool_calls`。
3. 检查最多两步和合法链路。
4. 执行真实 AMap 工具。
5. 回填 `role=tool` 消息。
6. 再要求模型给最终自然语言回答。

它也兼容 `<tool_call>...</tool_call>` 文本形式，会尝试解析成 tool_calls。

## 15. Stage1 自然语言评测

Stage1 冻结评测集：

```text
src/eval/eval_guide_generation.json
src/eval/eval_travel_qa.json
src/eval/eval_hotel_recommendation.json
src/eval/eval_traffic_planning.json
src/eval/eval_persona_understanding.json
src/eval/eval_multi_turn_dialogue.json
```

冻结名：

```text
stage1_main_eval_v1_frozen
```

规模：

- 6 类各 60 条
- 总计 360 条
- 与训练集做过精确隔离检查

运行远程推理：

```bash
python src/eval/scripts/run_eval.py \
  --base-url http://<server-ip>:7860/v1 \
  --api-key EMPTY \
  --model qwen3_32b_stage2_amap_tool_use \
  --model-name qwen3_32b_stage2_amap_tool_use \
  --run-name qwen3_32b_stage2_text_eval \
  --disable-thinking \
  --temperature 0 \
  --max-new-tokens 512
```

规则检查：

```bash
python src/eval/scripts/score_rules.py \
  --run-dir src/eval/reports/qwen3_32b_stage2_text_eval
```

LLM Judge：

```bash
export DASHSCOPE_API_KEY=<your-key>

python src/eval/scripts/judge_with_llm.py \
  --run-dir src/eval/reports/qwen3_32b_stage2_text_eval \
  --judge-model qwen-plus
```

收集 badcase：

```bash
python src/eval/scripts/collect_badcases.py \
  --run-dir src/eval/reports/qwen3_32b_stage2_text_eval \
  --output data/eval/stage1/stage1_badcases.jsonl \
  --summary-output data/eval/stage1/stage1_badcases_summary.json
```

Stage1 回归门槛建议：

- Stage2 后相对 Stage1-only 模型总体通过率下降不超过 5 个百分点。
- 重点看 `traffic_planning`、`persona_understanding`、`multi_turn_dialogue`。
- 修复实验不能只看 repair 类别变好，也要看其他类别有没有退化。

## 16. Stage2 工具调用评测

Stage2 专项评测目录：

```text
src/tool_eval/
```

关键文件：

| 文件 | 用途 |
|---|---|
| `src/tool_eval/datasets/native_tool_baseline.json` | 原生工具调用 baseline 小集 |
| `src/tool_eval/datasets/stage2_amap_golden.json` | Stage2 AMap golden，当前 50 条 |
| `src/tool_eval/datasets/stage2_amap_thinking_canary.json` | thinking-mode 兼容性观察 |
| `src/tool_eval/scripts/run_native_tool_baseline.py` | 原生 tool calling 基线 |
| `src/tool_eval/scripts/analyze_native_tool_baseline.py` | baseline 汇总 |
| `src/tool_eval/scripts/run_tool_eval.py` | Stage2 真实工具执行评测 |
| `src/tool_eval/scripts/score_tool_eval.py` | Stage2 指标打分 |

已有 native baseline 摘要：

- total cases: 6
- tool call cases: 4
- no-tool cases: 2
- argument JSON: 4/4 合法

这说明原生 Qwen 已经有可利用的 OpenAI-compatible tool calling 基础，Stage2 的重点是协议对齐。

运行 native baseline：

```bash
python src/tool_eval/scripts/run_native_tool_baseline.py \
  --base-url http://<server-ip>:7860/v1 \
  --api-key EMPTY \
  --model qwen3_32b_stage2_amap_tool_use

python src/tool_eval/scripts/analyze_native_tool_baseline.py
```

运行 Stage2 tool eval：

```bash
export AMAP_API_KEY=<your-amap-key>

python src/tool_eval/scripts/run_tool_eval.py \
  --base-url http://<server-ip>:7860/v1 \
  --api-key EMPTY \
  --model qwen3_32b_stage2_amap_tool_use

python src/tool_eval/scripts/score_tool_eval.py
```

默认输出：

```text
src/tool_eval/reports/stage2_amap_tool_eval_outputs.json
src/tool_eval/reports/stage2_amap_tool_eval_summary.json
```

Release gate：

| 指标 | 门槛 |
|---|---:|
| `tool_selection_accuracy` | `>= 0.90` |
| `argument_accuracy` | `>= 0.85` |
| `no_tool_accuracy` | `>= 0.90` |
| `fallback_accuracy` | `>= 0.85` |
| `execution_success_rate` | `>= 0.90` |

建议额外关注：

- `clarify_accuracy`
- `final_answer_grounded_rate`
- `overall_pass_rate`

## 17. 本地测试建议

全量测试：

```bash
pytest -q
```

如果只改 Stage2 数据/工具链，优先跑：

```bash
pytest \
  tests/test_tool_use_dataset.py \
  tests/test_tool_use_orchestrator.py \
  tests/test_stage2_amap_builder.py \
  tests/test_stage2_processed_final_merge.py \
  tests/test_stage2_single_tool_call_cleaner.py \
  tests/test_stage2_slot_filling_tool_call_cleaner.py \
  tests/test_stage2_clarify_then_call_cleaner.py \
  tests/test_stage2_tool_result_grounded_answer_cleaner.py \
  tests/test_stage2_tool_failure_fallback_cleaner.py \
  tests/test_stage2_tool_eval.py \
  -q
```

如果只改 Stage1 数据混合：

```bash
pytest \
  tests/test_data_mixer.py \
  tests/test_dataset_audit.py \
  tests/test_handler_*.py \
  -q
```

如果只改部署网关：

```bash
pytest tests/test_frontend_server.py tests/test_tool_use_orchestrator.py -q
```

## 18. 接手后的推荐执行顺序

### 18.1 只想快速摸清现状

1. 读本文。
2. 读 `README.md` 的当前定位。
3. 看 `data/final/stage_mix_report.json`。
4. 看 `data/final/stage2_amap_tool_use_report.json`。
5. 看 `configs/llamafactory_stage1_32b_formal_sft.yaml`。
6. 看 `configs/llamafactory_stage2_32b_amap_tool_use_formal_sft.yaml`。
7. 看 `docs/qwen3_32b_stage1_stage2_rollout_guide.md`。
8. 看 `docs/vllm_frontend_deployment.md`。

### 18.2 要从数据继续做

1. 不改协议时，优先补 `clarify_then_call` 和 `two_step_chain`。
2. 使用 `docs/stage2_amap_qwen_plus_data_generation_system_prompt.md` 给数据部门生成 raw。
3. 分桶运行 cleaner。
4. 跑 `finalize_stage2_processed.py`。
5. 检查 `stage2_amap_tool_use_report.json` 是否通过。
6. 跑 Stage2 相关单测。
7. 上传 `stage2_amap_tool_use_sft.json` 到云端 LLaMA-Factory data。

### 18.3 要从训练继续做

1. 确认 `/root/soulv_assets/models/modelscope/models/Qwen/Qwen3-32B` 存在或软链接正确。
2. 确认 `/root/llama-factory/data` 有 Stage1/Stage2 JSON。
3. 确认 dataset registration 已合并。
4. 运行 `scripts/00_doctor_envs.sh`。
5. 跑 `bash scripts/02_run_sft.sh stage1_32b smoke`。
6. 跑 `bash scripts/02_run_sft.sh stage1_32b formal`。
7. 跑 `bash scripts/04_merge_stage1_for_stage2.sh stage1_32b`。
8. 跑 `bash scripts/02_run_sft.sh stage2_amap_32b smoke`。
9. 跑 `bash scripts/02_run_sft.sh stage2_amap_32b formal`。
10. 跑 `bash scripts/06_merge_stage2_for_deploy.sh stage2_amap_32b`。

### 18.4 要从部署/验收继续做

1. 用 merged model 启 vLLM。
2. 用 gateway 暴露 `7860`。
3. 设置 `AMAP_API_KEY`。
4. 网页验证普通聊天、首轮 tool_calls、端到端工具执行。
5. 跑 `src/tool_eval`。
6. 跑 `src/eval` 自然语言回归。
7. 对照 release gate 和 Stage1 回归门槛出结论。

## 19. 常见问题排查

### 19.1 训练报 dataset 找不到

优先检查：

1. `/root/llama-factory/data/dataset_info.json` 是否注册 dataset 名。
2. YAML 里的 `dataset` 名是否一致。
3. JSON 文件是否真的在 `/root/llama-factory/data`。
4. `file_name` 是否和实际文件名一致。

### 19.2 训练报模型路径不存在

优先检查：

1. `model_name_or_path`
2. `adapter_name_or_path`
3. `export_dir`
4. 是否需要软链接到 `/root/soulv_assets/...`

### 19.3 32B smoke OOM

降级顺序：

1. 先降 `cutoff_len`。
2. 再降 Stage2 的 `cutoff_len`。
3. 最后考虑把 `lora_rank` 从 64 降到 32。

不要靠继续缩小数据量伪通过正式配置。

### 19.4 tool eval 全部工具失败

优先检查：

1. 服务机是否设置 `AMAP_API_KEY`。
2. 网关进程是否继承了这个环境变量。
3. 服务器是否能访问 `restapi.amap.com`。
4. 高德 key 是否有 Web Service 权限。

### 19.5 模型不会输出 tool_calls

优先检查 vLLM 参数：

1. 是否开启 `--enable-auto-tool-choice`。
2. 是否设置 `--tool-call-parser hermes`。
3. 是否模型请求里真的传了 `tools`。
4. 先用 `tool_choice=required` 手动验证，再测 `auto`。

### 19.6 Stage2 后自然语言回归掉了

优先排查：

1. Stage2 数据是否过度强调工具调用。
2. `no_tool_needed` 是否不足或质量差。
3. 训练轮数是否过多。
4. learning rate 是否过高。
5. 是否需要加入/提高 badcase repair 或自然语言混合回归。

### 19.7 评测里出现秒数/米数裸露

这是 Stage2 v1 已暴露过的问题。优先处理：

1. 增强 `tool_result_grounded_answer`。
2. 检查 cleaner 是否过滤裸露 `2537秒`、`18753米` 类表达。
3. 检查 final answer synthesis prompt。

## 20. 还没做完或建议后续补的事

建议下一位优先处理：

1. 更新 `scripts/01_run_pipeline.sh`，让它对齐当前 `raw_stage1/processed_stage1` 目录。
2. 更新 `scripts/05_build_stage2_amap_data.sh`，明确区分早期 builder 和当前 v2 processed finalization。
3. 若要继续提升 Stage2，优先补齐 `clarify_then_call` 和 `two_step_chain` 短缺样本。
4. 把 32B 云端真实状态补到一份非公开运维文档：服务器 IP、实例名、磁盘挂载、实际权重目录、实际 checkpoint 目录、已开放端口。
5. 给 `src/eval/` 补统一 report 生成脚本，目前已有 runner/rules/judge/badcase collector，但没有最终报告生成器。
6. 若 Stage2 v2 训练后，重新跑 50 条 golden 并归档 `stage2_amap_tool_eval_summary.json`。

## 21. 最小安全清单

任何人接手后，正式开训/部署前都要过这张表：

- [ ] 没有把 `AMAP_API_KEY`、`DASHSCOPE_API_KEY`、vLLM key 写进 Git。
- [ ] 云端 `/root/soulv_llm` 只放代码，不放大模型权重和大数据。
- [ ] 大资源都在 `/root/soulv_assets` 或明确软链接路径。
- [ ] 训练数据已放到 `/root/llama-factory/data`。
- [ ] dataset registration 已合并。
- [ ] `scripts/00_doctor_envs.sh` 正常。
- [ ] Stage1/Stage2 smoke 先过，再跑 formal。
- [ ] Stage2 后同时跑 tool eval 和 Stage1 回归。
- [ ] 所有评测报告和命令参数都归档，方便下一轮对比。

## 22. 快速命令索引

环境：

```bash
bash scripts/00_setup_lf_env.sh
bash scripts/00_setup_qwen_env.sh
bash scripts/00_prepare_aliyun_layout.sh
bash scripts/00_doctor_envs.sh
```

Stage1 32B：

```bash
bash scripts/02_run_sft.sh stage1_32b smoke
bash scripts/02_run_sft.sh stage1_32b formal
bash scripts/04_merge_stage1_for_stage2.sh stage1_32b
```

Stage1 repair：

```bash
bash scripts/02_run_sft.sh stage1_32b repair
bash scripts/04_merge_stage1_for_stage2.sh stage1_32b_repair
```

Stage2 32B：

```bash
bash scripts/02_run_sft.sh stage2_amap_32b smoke
bash scripts/02_run_sft.sh stage2_amap_32b formal
bash scripts/06_merge_stage2_for_deploy.sh stage2_amap_32b
```

Stage2 v2 finalization：

```bash
python src/data_pipeline/finalize_stage2_processed.py
```

vLLM：

```bash
MODEL_VARIANT=32b \
MODEL_PATH=/root/soulv_assets/runs/merged/qwen3_32b_stage2_amap_tool_use_merged \
TOKENIZER_PATH=/root/soulv_assets/models/modelscope/models/Qwen/Qwen3-32B \
SERVED_MODEL_NAME=qwen3_32b_stage2_amap_tool_use \
HOST=127.0.0.1 \
PORT=8000 \
bash scripts/03_run_vllm_api.sh
```

Gateway：

```bash
export AMAP_API_KEY=<your-amap-key>

HOST=0.0.0.0 \
PORT=7860 \
UPSTREAM_VLLM_BASE_URL=http://127.0.0.1:8000 \
DEFAULT_MODEL_NAME=qwen3_32b_stage2_amap_tool_use \
bash scripts/07_run_frontend_gateway.sh
```

Stage2 tool eval：

```bash
python src/tool_eval/scripts/run_tool_eval.py \
  --base-url http://<server-ip>:7860/v1 \
  --api-key EMPTY \
  --model qwen3_32b_stage2_amap_tool_use

python src/tool_eval/scripts/score_tool_eval.py
```

Stage1 regression：

```bash
python src/eval/scripts/run_eval.py \
  --base-url http://<server-ip>:7860/v1 \
  --api-key EMPTY \
  --model qwen3_32b_stage2_amap_tool_use \
  --model-name qwen3_32b_stage2_amap_tool_use \
  --disable-thinking

python src/eval/scripts/score_rules.py --run-dir <run-dir>

export DASHSCOPE_API_KEY=<your-key>
python src/eval/scripts/judge_with_llm.py --run-dir <run-dir>
```

本地测试：

```bash
pytest -q
```


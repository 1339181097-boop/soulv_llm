# Keep the vLLM serving environment isolated from the LLaMA-Factory training env.
# Qwen3 tool-calling needs Hermes function calling support (>=0.8.5) and the
# Qwen3 reasoning parser path used by this repo needs >=0.9.0.
vllm>=0.9.0,<0.10.0
